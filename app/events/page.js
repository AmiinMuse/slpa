import PagePlaceholder from "../components/PagePlaceholder";

export const metadata = { title: "Events — SLPA" };

export default function EventsPage() {
  return (
    <PagePlaceholder
      title="Events"
      description="Upcoming and past conferences. Content coming soon."
    />
  );
}
